
# Procure AI Workspace — Requirements (Databricks Apps runtime: Linux/Ubuntu 22.04, Python 3.11)
#
# HAND-MAINTAINED. Do NOT run `uv pip compile` on Windows and commit the result:
# it pins Windows-only wheels (pywin32, win32-setctime) with no platform markers,
# which breaks the Databricks Apps build ("No matching distribution found for pywin32").
# If a full lock is ever needed, use: uv pip compile pyproject.toml --python-platform linux
#
# Keep constraints loose; let pip resolve on the Linux build host.

# Core AI Framework
langchain>=1.3.14,<2.0
langchain-core>=1.5.3,<2.0
langgraph>=1.2.10,<2.0
deepagents>=0.7.5
databricks-langchain
langgraph-checkpoint-sqlite
aiosqlite

# API & Web Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=1.8.0
flask>=3.0.0
flask-cors>=6.0.0
gunicorn>=22.0.0
# Windows-only fallback WSGI server for ops/deployment/run_app.py (gunicorn
# needs POSIX `fcntl`). Pure Python — harmless on the Linux Apps build host.
waitress>=3.0.0
httpx>=0.27.0
python-multipart>=0.0.9

# Configuration & Validation
python-dotenv>=1.0.0
pydantic>=2.0.0
PyYAML>=6.0.0

# LangChain Components
langchain-openai>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# Vector Database
faiss-cpu>=1.8.0

# Logging & Observability
loguru>=0.7.2
mlflow>=3,<4

# SQLAlchemy for ORM (existing web_app)
sqlalchemy>=2.0.52
psycopg[binary]>=3.1

# Testing
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0

# Code quality
ruff>=0.5.0,<1.0

# Databricks SDK & utilities
databricks-sdk==0.125.0
urllib3>=2.2.0,<3.0
